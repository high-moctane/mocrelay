pre-commit:
  commands:
    fix:
      glob: "*.go"
      env:
        GOEXPERIMENT: jsonv2
      run: go fix ./...
      stage_fixed: true
    fmt:
      glob: "*.go"
      run: gofmt -s -w {staged_files}
      stage_fixed: true
